import React from "react";
import Navbar from "../Navbar";

export const AlgsPage = (props) => {
    return (
        <div>
            <div>
                <h1>Algs from Database</h1>
            </div>
        </div>
    );
};
